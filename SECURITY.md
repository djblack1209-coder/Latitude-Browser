# Security

Latitude Browser 管理本地浏览器进程、配置与代理。当前项目仍处于持续开发阶段，没有承诺安全响应时限或受支持的稳定发行版。

## 报告敏感问题

如果仓库的 **Security → Report a vulnerability** 入口可用，请使用 GitHub 的私密漏洞报告。若入口不可用，可以先提交不含利用细节和私人信息的 Issue，请求建立私密联系渠道；不要在公开 Issue 中粘贴凭据或可直接利用的攻击步骤。

可提供受影响的提交、平台、最小复现与影响说明。使用虚构测试数据替代真实账户。

## 使用边界

- Node 自动化脚本可访问当前用户的文件、网络和系统命令。执行超时与终止进程不构成权限沙箱。
- 浏览器配置目录和备份可能包含会话、Cookie 与代理凭据，不适合公开分享。
- 本地 API / CDP 入口应保留在受控的本机环境；不要将其直接暴露到公共网络。
- 指纹参数和独立目录不是匿名性、安全隔离或规避检测的保证。
- 浏览器内核和代理运行时是独立供应链组件。仓库清单中的哈希用于完整性校验，不代表已完成漏洞审计或发布者签名验证。
