# 五分钟演示 Latitude Browser

[返回首页](../../README.md) · [工程导览](ENGINEERING.md)

适合项目分享或面试介绍。先说明这是基于上游项目持续改造的桌面工作台，再展示你能解释的具体改动。不要把原有功能、AI 辅助实现或上游内核说成全部独立开发。

## 演示顺序

| 时间 | 展示 | 讲解重点 |
| --- | --- | --- |
| 0:00–0:40 | README 与实例页 | 多环境测试时，配置、代理和脚本分散；工作台把它们放到实例这一共同入口 |
| 0:40–1:40 | 自动配置 | 网络入口 → 设备基线 → 指纹策略 → 确认；解释为什么避免让新用户直接编辑所有参数 |
| 1:40–2:30 | 连接栈设置 | Xray + sing-box 是组合栈，Mihomo 是独立栈；测速和启动必须遵守同一个选择 |
| 2:30–4:00 | 工程导览与一个提交 | 选择自己理解最清楚的改动，讲触发条件、设计取舍、代码和验证 |
| 4:00–5:00 | 测试与 Roadmap | 区分 UI 预览、Go 测试、桌面集成和发布；诚实说明未完成的环节 |

## 两种演示环境

**浏览器 UI 预览：** 按 README 启动 Vite。可讲解页面与配置结构；画面保留 `UI PREVIEW` 提示。不要把模拟启动、测速或状态变化说成实际浏览器运行。

**原生应用演示：** 仅在你已经完成自己的实机验证后使用。先准备空白演示实例和可信本地测试页，依次完成创建、启动、查看出口、停止与清理。macOS 使用 `/Applications/Latitude Browser.app`；不要使用工作资料、登录会话或私人代理作为演示素材。

## 这一组 README 截图

- 源码基线：`e90e35d`。
- 渲染：独立 Vite 服务与独立 Chromium 浏览器会话。
- 页面：`/browser/list`、`/browser/auto-config`、`/settings?section=connectors`。
- 数据：四个虚构且停止的测试环境，来自 [截图用演示数据](../../frontend/scripts/showcase-fixtures.mjs)。
- 画面未做 UI 拼接，保留 UI 预览说明；不使用私人账号、订阅或真实代理出口。

开发者如需重建演示数据，可以在 Vite 预览的浏览器控制台调用：

```js
const { seedShowcase } = await import('/scripts/showcase-fixtures.mjs')
await seedShowcase()
```

然后点击实例页的“刷新”。数据只写入预览模块的内存；刷新整个页面即可恢复默认值。该脚本遇到桌面桥接会拒绝执行。

## 准备一个可被追问的例子

选择 [工程导览中的提交](ENGINEERING.md)，准备四句话：原先哪一步有问题、为什么选这种实现、怎样验证、还有什么限制。若用了代码生成工具，说明自己负责的需求判断、代码阅读和验证。项目价值来自你能解释并维护的行为，不需要夸大 Star 数量或生产使用规模。
