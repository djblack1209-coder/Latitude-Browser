# Latitude Browser skills

- `latitude-browser-openclaw/` is the canonical OpenClaw integration skill.
- `legacy/` contains compatibility-only historical skills for migration of existing installations. New integrations and tests must not use those entries.
